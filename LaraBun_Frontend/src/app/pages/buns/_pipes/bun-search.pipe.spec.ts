import { BunSearchPipe } from './bun-search.pipe';

describe('BunSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BunSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
